apiVersion: apps/v1
kind: Deployment
metadata:
  name: compliance-audit-agents
  labels:
    app: compliance-audit-agents
    project: ai-engineer-portfolio
spec:
  replicas: 1
  selector:
    matchLabels:
      app: compliance-audit-agents
  template:
    metadata:
      labels:
        app: compliance-audit-agents
    spec:
      containers:
        - name: compliance-audit-agents
          image: compliance-audit-agents:latest
          ports:
            - containerPort: 8000
              protocol: TCP
          env:
            - name: ENVIRONMENT
              value: "production"
            - name: LOG_LEVEL
              value: "INFO"
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: compliance-audit-secrets
                  key: openai-api-key
                  optional: true
            - name: ANTHROPIC_API_KEY
              valueFrom:
                secretKeyRef:
                  name: compliance-audit-secrets
                  key: anthropic-api-key
                  optional: true
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 30
---
apiVersion: v1
kind: Service
metadata:
  name: compliance-audit-agents
  labels:
    app: compliance-audit-agents
spec:
  type: ClusterIP
  ports:
    - port: 8012
      targetPort: 8000
      protocol: TCP
  selector:
    app: compliance-audit-agents
